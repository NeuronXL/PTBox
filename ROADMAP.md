# PTBox 实现路线图

更新日期：2026-09-22。

发布进展：已获准将源码和 v1.1.1 上传至 NeuronXL/PTBox，重新构建和 26 组测试已通过。下文“不初始化 Git、不上传”为此前阶段记录，已由本次发布授权替代。真实跨版本安装升级仍需干净环境验收。

## 当前基线：已交付

- [x] Windows 11 x64 原生 WPF 大屏 Launcher。
- [x] 动态首页、空状态、可选 Steam / Moonlight 模板、用户应用管理。
- [x] 原应用图标、网站图标缓存、统一设置和操作弹窗。
- [x] 应用启动、进程复用、退出返回、热键、单实例和系统入口。
- [x] 本地配置保护、日志、应用测试与界面渲染验证。
- [x] 1.0.0 便携版和中文版安装包；手动覆盖升级保留配置。
- [x] 项目文档整理与下一阶段方案归档。
- [x] 1.0.1 网页地址输入容错、保存校验定位与旧提示清理。

当前应用测试 26 组；历史 1.0.0 安装基线为 16 项检查。1.1.1 已修复更新错误分类和限流冷却，安装升级仍待干净环境验证。见 [验证记录](docs/verification.md) 和 [发布更新操作](docs/release-operations.md)。

## 当前目标 M2：GitHub Releases 发布式在线更新

**优先级：P0。状态：本地实现完成，真实安装升级与线上验收待完成。**

目标：维护者发布正式 GitHub Release 后，已接入更新功能的 PTBox 能检查版本、展示说明、下载并验证完整安装包，经用户确认退出并升级，自动回到首页，保留个人数据。

采用现有 Inno Setup 全量安装包和独立更新进程；应用重启生效，不重启 Windows。方案见 [详细设计](docs/release-update-plan.md)。当前 1.0.0 需要手动安装一次带更新功能的版本，之后才具备在线升级能力。

### 实现顺序

| 阶段 | 交付内容 | 完成条件 | 状态 |
| --- | --- | --- | --- |
| M2.1 数据与版本基础 | 安装标记、版本来源、备份与迁移、便携模式 | 迁移冲突、资源和未来格式保护测试通过 | 已实现并本地验证 |
| M2.2 发布协议与打包 | 清单、RSA-PSS 签名、干净打包、本地发布脚本 | 签名与包可自验，私钥不入发行物 | 已实现 |
| M2.3 客户端更新服务 | GitHub 检查、数字版本比较、下载、校验和缓存 | 模拟网络与错误场景测试通过 | 已实现并本地验证 |
| M2.4 更新 UI 与执行器 | 更新页、进度、独立进程、安装和启动确认 | UI 渲染和导航已验证；实际安装交接待干净环境 | 已实现，安装验收待完成 |
| M2.5 GitHub 发布接入 | 固定 NeuronXL/PTBox、本地签名产物 | 用户要求先不初始化 Git、不上传 | 更新源已配置，线上发布未执行 |
| M2.6 端到端验收与交付 | 沙盒两版本升级、失败恢复、真实下载 | 当前主机缺少沙盒且安装测试受保护 | 验收脚本已提供，实测待完成 |

### 验收标准

- [ ] 对指定 GitHub 仓库检查正式版本，忽略草稿/预发布，不自动降级。
- [x] 设置页展示当前版本、更新说明、检查状态、进度和重试入口。
- [ ] 自动检查限频且不抢焦点，离线仍可正常使用；升级必须由用户确认。
- [ ] 签名、平台、版本、文件大小、SHA256 任一不符，均不执行安装包。
- [ ] Launcher 正常退出后再安装到原位置，完成后只启动一个新版实例。
- [ ] 不结束游戏/串流等外部应用，不修改用户的关机、重启或登录启动选择。
- [x] 应用配置、排序、图标、壁纸、最后选择和已有偏好保留；迁移失败保留原始数据（本地迁移测试）。
- [ ] 安装失败有日志与恢复说明；不把覆盖安装宣传为自动事务回滚。
- [ ] 发布产物不含个人配置、日志、缓存、令牌或签名私钥。
- [ ] 本地自动化验证、真实 GitHub 下载、客厅电脑更新各有对应结果，不混为一项。

### 剩余发布与验收条件

用户已创建公开仓库 `https://github.com/NeuronXL/PTBox`，指定只使用此地址开发更新功能，不初始化本地 Git、不上传。接入版为 1.1.0，签名私钥由本机 DPAPI 加密保存在忽略目录，客户端只编入公钥。

后续需在干净 Windows 11 环境执行安装和两版本升级，并在用户允许正式发布后验证真实 GitHub 下载。当前不能宣称线上升级已经闭环。

## 后续候选（排在 M2 之后）

| 优先级 | 方向 | 说明 |
| --- | --- | --- |
| P1 | XInput 与遥控器诊断 | A/B 映射、摇杆死区、长按节奏、特殊 HID 诊断 |
| P1 | 应用发现与路径修复 | 已安装软件发现、失效路径引导，继续坚持用户选择后才添加 |
| P2 | 窗口级生命周期和托盘 | 改善常驻进程、启动器交接、多窗口返回 |
| P2 | 差分更新与更完善的恢复 | 在全量更新稳定后评估 Velopack 等方案，另行设计迁移 |

天气、硬件监控、游戏封面管理、账户与云同步不纳入当前目标。
