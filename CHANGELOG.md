# 版本记录

## 1.1.1 — 2026-09-22

- 修复 HTTP 403 被统一误判为 GitHub 限流；依据状态码、响应头及有限的 JSON 错误正文区分访问拒绝与限流。
- 遵守 Retry-After / X-RateLimit-Reset，显示本地可重试时间，跨重启保存冷却；普通网络错误仍可主动重试。
- 无正式 Release 时确认仓库可访问并显示“暂无正式版本”，避免误报已是最新版。
- 更新检查和下载错误写入 launcher.log，包括 HTTP 状态、限流头及请求标识，不记录原始错误页。

## 1.1.0 — 2026-09-22（本地待发布）

- 更新源固定为 `NeuronXL/PTBox`，新增“关于与更新”、后台限频检查、说明、下载进度、取消和重试。
- 新增独立更新器、安装登记及父进程身份验证、安装前签名和哈希复验、新版启动确认及失败日志；升级不重启 Windows。
- RSA-PSS 签名清单、受限 HTTPS 重定向、包大小与 SHA256 校验、ETag 缓存和已下载文件复用。
- 安装标记、个人数据迁移、双配置冲突选择、相对资源处理、迁移前/更新前备份及未来格式写回保护。
- 本地发行脚本、DPAPI 加密发行私钥、沙盒升级验收脚本；没有初始化 Git，没有上传或正式发布。
- 本地应用和模拟网络测试通过；真实安装/升级及 GitHub 下载尚待验收，见 [操作与边界](docs/release-operations.md)。

## 1.0.1 — 2026-09-22

- 网页输入兼容 HTTP(S) 开头的全角冒号和斜杠；清理地址两端的空白及常见不可见粘贴标记，不改写路径、查询参数或片段中的字符。
- 空网页 / 应用协议地址不能再从设置页保存；启动与保存使用同一地址校验。
- 校验错误注明应用名称，自动选中有问题的应用并聚焦地址框；校验失败不写入配置。
- 重新编辑地址时清除旧的保存失败提示，避免修正后仍显示过时错误。
- 网页和协议入口隐藏只适用于 EXE 的“浏览”按钮。
- 新增真实 WPF 地址输入、跨应用错误定位、修正后保存及无效地址不落盘的回归测试。
- 标准 `https://www.baidu.com` 经实际输入框保存测试通过。用户截图中的具体输入字符未能读取，因此没有将本次原始报错归因为已确认的全角字符。

## 1.0.0 — 2026-09-22

首次安装版：大屏首页、用户应用管理、原图标、统一设置及弹窗、进程启动与返回、本地配置、自包含便携版和中文安装包。支持手动覆盖安装保留配置，尚无在线更新。
